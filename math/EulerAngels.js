///////////////欧拉角
function EulerAngels(yaw, pitch, rol) {
    this.yaw = yaw;   // 绕
    this.pitch = pitch;
    this.rol = rol;
}

EulerAngels.prototype.fromRotationMatrix = function () {
    
};

EulerAngels.prototype.fromWorldToObjectMatrix = function()
{
    
};